export type Scene         = "idle" | "whoami" | "fieldops" | "dimmed";
export type ShutdownPhase = "idle" | "killing" | "collapsing" | "minimizing" | "done";

export const WHO_AMI_IDX   = 4;
export const FIELD_OPS_IDX = 5;
export const CONTACT_IDX   = 6;
